name: flutter_offline_online
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  http: ^0.13.0
  sqflite: ^2.3.0
  path_provider: ^2.0.9
  path: ^1.8.3
  flutter_offline: "^3.0.1"
  flutter_bloc: ^8.1.6
  equatable: ^2.0.5
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
